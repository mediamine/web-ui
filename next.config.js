/** @type {import('next').NextConfig} */
const nextConfig = {
  // TODO: comment the below when running a local production server without docker
  output: 'standalone'
};

module.exports = nextConfig;
